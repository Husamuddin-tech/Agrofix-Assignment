import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#388e3c",
    },
    secondary: {
      main: "#f57c00",
    },
  },
});

export default theme;
